# =================================================================
# Stationly Syncer - Remote Environment Overrides
# =================================================================

# TfL API Configuration
# Remote forces environment key lookup (no fallback/default key)
tfl.app.key=${TFL_APP_KEY:bff7e80b234d440d9fea4a1b3b96fae4}

# Firebase & Firestore Configuration
# Remote forces firestore project-id lookup (no fallback)
firestore.project-id=${FIRESTORE_PROJECT_ID:mindthetimefcm}
